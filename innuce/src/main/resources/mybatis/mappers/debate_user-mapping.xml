<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mc.innuce.domain.debate.dao.DebateUserDAO">
	<select id="selectOneDebateUserByData" parameterType="debateuserdto" resultType="debateuserdto">
		select *
		from debate_user
		where user_key = #{user_key} and debate_room_key = #{debate_room_key}
	</select>
	
	<insert id="insertDebateUser" parameterType="debateuserdto">
		insert into debate_user(user_key, debate_room_key)
		values (#{user_key}, #{debate_room_key})
	</insert>
	
	<select id="selectOneUserId" parameterType="int" resultType="String">
		select user_id
		from user_info
		where user_key = (select user_key from debate_user where debate_user_key = #{debate_user_key});
	</select>
	
	<select id="selectUserIdList" parameterType="java.util.List" resultType="String">
		<foreach collection="debateUserKeyList" item="debate_user_key" separator=" union all ">
			select user_id
			from user_info
			where user_key = (select user_key from debate_user where debate_user_key = #{debate_user_key})
		</foreach>
	</select>
</mapper>